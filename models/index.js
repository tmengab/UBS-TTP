const Concept = require('./concept');
const Question = require('./question');
const Material = require('./material');
const UserProgress = require('./UserProgress');

module.exports = { Concept, Question, Material, UserProgress };
